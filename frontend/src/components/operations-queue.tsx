"use client";
/* Date.now is used only inside an explicit operator click handler. */
/* eslint-disable react-hooks/purity */
import Link from "next/link";import{useMemo,useState}from"react";import{publicBackend}from"@/lib/api";
export type QueueItem={case_id:string;case_version:number;state:string;priority:string;category:string;amount_at_risk_minor:number;currency:string;merchant_name:string;customer_safe_reference:string;leak_type:string;recommended_action:string;expected_nerv_minor:number;incremental_uplift:number;escalation_reasons:string[];recovery_deadline:string;review_status:string};
const money=(n:number,c="INR")=>new Intl.NumberFormat("en-IN",{style:"currency",currency:c,maximumFractionDigits:0}).format(n/100);
export function OperationsQueue({initial}:{initial:QueueItem[]}){const[items,setItems]=useState(initial),[priority,setPriority]=useState("ALL"),[busy,setBusy]=useState(""),[message,setMessage]=useState("");const shown=useMemo(()=>items.filter(x=>priority==="ALL"||x.priority===priority),[items,priority]);async function review(item:QueueItem,decision:"APPROVE"|"REJECT"|"DEFER"|"STOP"){const operator=window.prompt("Operator ID (stored in the immutable audit trail)","demo-operator");if(!operator)return;const notes=window.prompt("Review notes","")??"";setBusy(item.case_id);const body:{[key:string]:unknown}={decision,operator_id:operator,actor_type:"OPERATOR",reason_code:`OPERATOR_${decision}`,notes,expected_case_version:item.case_version,idempotency_key:`ui:${item.case_id}:${decision}:${crypto.randomUUID()}`};if(decision==="DEFER")body.review_after=new Date(Date.now()+3600000).toISOString();try{const response=await fetch(`${publicBackend}/api/v1/operations/recovery-queue/${item.case_id}/review`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await response.json();if(!response.ok)throw new Error(data.detail??data.error??"Review failed");setMessage(`${item.case_id}: ${data.next_step}`);if(decision==="APPROVE"||decision==="STOP")setItems(v=>v.filter(x=>x.case_id!==item.case_id));else setItems(v=>v.map(x=>x.case_id===item.case_id?{...x,review_status:decision}:x))}catch(error){setMessage(error instanceof Error?error.message:"Review failed")}finally{setBusy("")}}return <><div className="queueTools"><label>Priority <select value={priority} onChange={e=>setPriority(e.target.value)}><option>ALL</option><option>CRITICAL</option><option>HIGH</option><option>NORMAL</option></select></label><span>{shown.length} reviewable cases</span></div>{message&&<div className="notice">{message}</div>}<section className="queue">{shown.map(item=><article key={item.case_id} className="queueCard"><div><span className={`priority ${item.priority.toLowerCase()}`}>{item.priority}</span><small>{item.category.replaceAll("_"," ")}</small><h2>{money(item.amount_at_risk_minor,item.currency)} at risk</h2><p>{item.merchant_name} · {item.customer_safe_reference} · {item.leak_type.replaceAll("_"," ")}</p></div><div className="queueEvidence"><span>Recommended <b>{item.recommended_action.replaceAll("_"," ")}</b></span><span>Expected NERV <b>{money(item.expected_nerv_minor,item.currency)}</b></span><span>Incremental uplift <b>{(item.incremental_uplift*100).toFixed(1)}%</b></span><span>Deadline <b>{new Date(item.recovery_deadline).toLocaleString()}</b></span></div><details><summary>Why this needs a human</summary><ul>{item.escalation_reasons.map(x=><li key={x}>{x.replaceAll("_"," ")}</li>)}</ul></details><div className="reviewActions"><button disabled={busy===item.case_id} onClick={()=>review(item,"APPROVE")}>Approve safely</button><button disabled={busy===item.case_id} onClick={()=>review(item,"DEFER")}>Defer</button><button disabled={busy===item.case_id} onClick={()=>review(item,"REJECT")}>Reject</button><button disabled={busy===item.case_id} onClick={()=>review(item,"STOP")}>Stop case</button><Link href={`/recovery/${item.case_id}`}>Full replay →</Link></div></article>)}</section>{shown.length===0&&<div className="empty panel">No cases match this review queue.</div>}</>}
