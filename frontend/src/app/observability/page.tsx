import{Nav}from"@/components/nav";import{backendJSON}from"@/lib/api";type S={generated_at:string;schema_version:string;queue:{pending:number;running:number;failed:number;max_lag_seconds:number};execution:{succeeded:number;failed:number;timed_out:number;retrying:number};recovery:{active_cases:number;recovered_cases:number;escalated_cases:number;stopped_cases:number;expired_promises:number};alerts:{code:string;severity:string;message:string}[]};
export default async function Observability(){const x=await backendJSON<S>("/api/v1/observability");return <><Nav/><main className="shell"><section className="pageIntro"><div className="eyebrow">OPERATIONAL HEALTH</div><h1>See the system.<br/><em>Before it surprises you.</em></h1><p>Durable queue lag, execution outcomes, recovery states, promises, schema posture, and derived alerts from persisted truth.</p></section>{!x?<div className="notice">Observability snapshot unavailable. Check backend readiness and migrations.</div>:<><section className="kpis"><article><small>Queue pending / running</small><strong>{x.queue.pending} / {x.queue.running}</strong><span>Max lag {Math.round(x.queue.max_lag_seconds)}s</span></article><article><small>Execution success / failed</small><strong>{x.execution.succeeded} / {x.execution.failed}</strong><span>{x.execution.retrying} retrying · {x.execution.timed_out} timeout</span></article><article><small>Active / recovered</small><strong>{x.recovery.active_cases} / {x.recovery.recovered_cases}</strong><span>{x.recovery.escalated_cases} escalated</span></article><article><small>Schema</small><strong>{x.schema_version}</strong><span>{new Date(x.generated_at).toLocaleString()}</span></article></section><section className="panel alertPanel"><div className="panelTitle"><h2>Active alerts</h2><span>{x.alerts.length}</span></div>{x.alerts.length?x.alerts.map(a=><div className={`alert ${a.severity}`} key={a.code}><b>{a.code.replaceAll("_"," ")}</b><span>{a.message}</span></div>):<div className="healthy">No thresholds breached</div>}</section></>}</main></>}
